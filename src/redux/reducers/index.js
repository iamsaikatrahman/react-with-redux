import changeTheNumber from "./changeTheNumber";
import { combineReducers } from "redux";

const rootReducer = combineReducers({
  // ADD ALL REDUCERS ONE BY ONE
  changeTheNumber,
});

export default rootReducer;
